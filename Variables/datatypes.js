let age=22
let name="Nipun"
let isSingle=true

console.log(typeof age);
console.log(typeof name);
console.log(typeof isSingle);
/*basic data types above*/

/*unique data types and values*/
/*for const values assignment required in the same line where they are declared*/
let a;
//undefined value for when a variable is declared but not assigned any value
//undefined is implicit way of showing no value is assigned while null is deliberate way
console.log(typeof a);

let b=null;
console.log(typeof(b));
console.log(null-undefined);
//Nan not a number is the result of a mathematical operation which couldn't produce a meaningful value
console.log("nipun"*1);